#include "lexer.hpp"
#include <iostream>
#include <string>

Token ParseNext(std::string fileData, Cursor* cursor) {
    Token result;
    TokenType type;
    Token* tokens[] = {};
    int dataInt;
    std::string dataString;
}
